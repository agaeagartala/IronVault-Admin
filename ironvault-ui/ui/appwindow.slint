// Placeholder Slint UI file for the IronVault Admin interface.
export component AppWindow {
    width: 1024px;
    height: 768px;
    Rectangle {
        width: parent.width;
        height: parent.height;
        color: "#1e1e2f";
    }
}
