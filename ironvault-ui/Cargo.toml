[package]
name = "ironvault-ui"
version = "0.1.0"
edition = "2021"

[dependencies]
# Connects our UI to our backend workspace dependency
ironvault-core = { path = "../ironvault-core" }
slint = "1.3"
tokio = { version = "1.0", features = ["full"] }

[build-dependencies]
slint-build = "1.3"